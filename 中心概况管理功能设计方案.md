# 中心概况管理功能设计方案

## 📋 功能概述

中心概况管理用于管理"中心概况"页面的所有内容，包括：
1. **Banner管理** - 首页Banner图片/视频
2. **详情管理** - 中心详情页的主要内容（视频、标题、建设背景、现状概况、发展愿景）
3. **组织架构管理** - 组织架构人员信息（主任、副主任、成员）
4. **下设实验室管理** - 中心下设的实验室列表

---

## 🗄️ 数据库设计

### 1. center_overview_banner 表（中心概况Banner）

存储中心概况页面的Banner信息。

```sql
CREATE TABLE center_overview_banner (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    type VARCHAR(20) NOT NULL DEFAULT 'image' COMMENT '类型：image-图片，video-视频',
    image_url VARCHAR(500) DEFAULT NULL COMMENT '图片路径（相对路径）',
    video_url VARCHAR(500) DEFAULT NULL COMMENT '视频路径（相对路径）',
    video_url_external VARCHAR(500) DEFAULT NULL COMMENT '外部视频URL',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

**说明：**
- Banner只有一条记录（单例模式）
- `type` 为 `image` 时使用 `image_url`
- `type` 为 `video` 时使用 `video_url` 或 `video_url_external`

---

### 2. center_overview_detail 表（中心概况详情）

存储中心概况页面的详情内容。

```sql
CREATE TABLE center_overview_detail (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    video_url VARCHAR(500) DEFAULT NULL COMMENT '视频路径（相对路径）',
    video_url_external VARCHAR(500) DEFAULT NULL COMMENT '外部视频URL',
    main_title VARCHAR(200) NOT NULL COMMENT '主标题',
    background TEXT COMMENT '建设背景（HTML格式）',
    overview TEXT COMMENT '现状概况（HTML格式）',
    vision TEXT COMMENT '发展愿景（HTML格式）',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

**说明：**
- 详情只有一条记录（单例模式）

---

### 3. center_overview_organization 表（组织架构）

存储组织架构人员信息。

```sql
CREATE TABLE center_overview_organization (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    role VARCHAR(50) NOT NULL COMMENT '角色：director-主任，deputy_director-副主任，member-成员',
    name VARCHAR(100) NOT NULL COMMENT '姓名',
    sort_order INT DEFAULT 0 COMMENT '排序顺序',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_role (role),
    INDEX idx_sort_order (sort_order)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

**说明：**
- 支持多个主任、副主任、成员
- 通过 `role` 字段区分角色
- 通过 `sort_order` 排序

---

### 4. center_overview_laboratory 表（下设实验室）

存储中心下设的实验室信息。

```sql
CREATE TABLE center_overview_laboratory (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(200) NOT NULL COMMENT '实验室名称',
    image_url VARCHAR(500) NOT NULL COMMENT '图片路径（相对路径）',
    link VARCHAR(500) DEFAULT NULL COMMENT '链接地址',
    sort_order INT DEFAULT 0 COMMENT '排序顺序',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_sort_order (sort_order)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

---

## 🔌 后端API设计

### 1. Banner管理 API

#### 1.1 获取Banner
```
GET /api/center-overview/banner
响应：
{
  "success": true,
  "data": {
    "id": 1,
    "type": "image",
    "imageUrl": "upload/20251130/xxx.jpg",
    "videoUrl": null,
    "videoUrlExternal": null,
    "createdAt": "2025-01-01T00:00:00",
    "updatedAt": "2025-01-01T00:00:00"
  }
}
```

#### 1.2 保存/更新Banner
```
POST /api/center-overview/banner
Content-Type: multipart/form-data
请求体：
- type: image | video
- image: File (当type=image时)
- video: File (当type=video时，可选)
- videoUrlExternal: String (当type=video且使用外部URL时)

响应：
{
  "success": true,
  "message": "保存成功",
  "data": { ... }
}
```

#### 1.3 删除Banner
```
DELETE /api/center-overview/banner
响应：
{
  "success": true,
  "message": "删除成功"
}
```

---

### 2. 详情管理 API

#### 2.1 获取详情
```
GET /api/center-overview/detail
响应：
{
  "success": true,
  "data": {
    "id": 1,
    "videoUrl": null,
    "videoUrlExternal": null,
    "mainTitle": "东南大学建筑学院实验教学中心",
    "background": "<p>建设背景内容...</p>",
    "overview": "<p>现状概况内容...</p>",
    "vision": "<p>发展愿景内容...</p>",
    "createdAt": "2025-01-01T00:00:00",
    "updatedAt": "2025-01-01T00:00:00"
  }
}
```

#### 2.2 保存/更新详情
```
POST /api/center-overview/detail
Content-Type: multipart/form-data
请求体：
- video: File (可选)
- videoUrlExternal: String (可选)
- mainTitle: String (必填)
- background: String (HTML格式)
- overview: String (HTML格式)
- vision: String (HTML格式)

响应：
{
  "success": true,
  "message": "保存成功",
  "data": { ... }
}
```

---

### 3. 组织架构管理 API

#### 3.1 获取组织架构
```
GET /api/center-overview/organization
响应：
{
  "success": true,
  "data": {
    "director": [
      { "id": 1, "name": "王伟", "sortOrder": 0 }
    ],
    "deputyDirector": [
      { "id": 2, "name": "华好", "sortOrder": 0 },
      { "id": 3, "name": "冯世虎", "sortOrder": 1 }
    ],
    "members": [
      { "id": 4, "name": "是罪", "sortOrder": 0 },
      { "id": 5, "name": "李超明", "sortOrder": 1 }
    ]
  }
}
```

#### 3.2 保存组织架构
```
POST /api/center-overview/organization
Content-Type: application/json
请求体：
{
  "director": ["王伟"],
  "deputyDirector": ["华好", "冯世虎"],
  "members": ["是罪", "李超明", "兰祥启", "刘宇衡", "周海飞"]
}

响应：
{
  "success": true,
  "message": "保存成功"
}
```

**说明：**
- 此接口会先删除所有现有数据，然后插入新数据
- 至少需要一位主任

---

### 4. 实验室管理 API

#### 4.1 获取实验室列表
```
GET /api/center-overview/laboratory
响应：
{
  "success": true,
  "data": [
    {
      "id": 1,
      "name": "设计基础实训实验室",
      "imageUrl": "upload/20251130/xxx.jpg",
      "link": "/lab/design-basic",
      "sortOrder": 1,
      "createdAt": "2025-01-01T00:00:00",
      "updatedAt": "2025-01-01T00:00:00"
    }
  ]
}
```

#### 4.2 添加实验室
```
POST /api/center-overview/laboratory
Content-Type: multipart/form-data
请求体：
- name: String (必填)
- image: File (必填)
- link: String (可选)
- sortOrder: Integer (可选，默认0)

响应：
{
  "success": true,
  "message": "添加成功",
  "data": { ... }
}
```

#### 4.3 更新实验室
```
PUT /api/center-overview/laboratory/{id}
Content-Type: multipart/form-data
请求体：
- name: String (可选)
- image: File (可选)
- link: String (可选)
- sortOrder: Integer (可选)

响应：
{
  "success": true,
  "message": "更新成功",
  "data": { ... }
}
```

#### 4.4 删除实验室
```
DELETE /api/center-overview/laboratory/{id}
响应：
{
  "success": true,
  "message": "删除成功"
}
```

---

## 📁 后端代码结构

### 实体类（Entity）

1. `CenterOverviewBanner.java`
2. `CenterOverviewDetail.java`
3. `CenterOverviewOrganization.java`
4. `CenterOverviewLaboratory.java`

### Repository接口

1. `CenterOverviewBannerRepository.java`
2. `CenterOverviewDetailRepository.java`
3. `CenterOverviewOrganizationRepository.java`
4. `CenterOverviewLaboratoryRepository.java`

### Service层

1. `CenterOverviewService.java` - 统一的服务类，包含所有业务逻辑

### Controller层

1. `CenterOverviewController.java` - 统一的控制器，包含所有API端点

---

## 🎨 前端功能设计

### 1. Banner管理

**功能：**
- 支持图片或视频类型
- 图片上传（文件上传到服务器）
- 视频上传（文件上传到服务器）
- 外部视频URL输入
- 预览功能
- 删除功能

**实现：**
- 使用现有的 `BannerManagement` 组件
- 调用文件上传API

---

### 2. 详情管理

**功能：**
- 视频Banner（上传文件或外部URL）
- 主标题输入
- 建设背景（富文本编辑器）
- 现状概况（富文本编辑器）
- 发展愿景（富文本编辑器）
- 保存和重置功能

**实现：**
- 使用 `RichTextEditor` 组件
- 视频上传使用文件上传API

---

### 3. 组织架构管理

**功能：**
- 主任列表（可添加/删除，至少保留1个）
- 副主任列表（可添加/删除）
- 成员列表（可添加/删除）
- 保存和重置功能

**实现：**
- 动态添加/删除表单
- 调用组织架构API

---

### 4. 实验室管理

**功能：**
- 实验室列表展示（表格）
- 添加实验室（对话框表单）
- 编辑实验室（对话框表单）
- 删除实验室（确认对话框）
- 图片上传（文件上传到服务器）
- 排序功能

**实现：**
- 使用 `el-table` 展示列表
- 使用 `el-dialog` 实现添加/编辑
- 使用文件上传API

---

## 🔧 实现要点

### 1. 文件上传

- 所有图片和视频都使用文件上传API
- 保存到 `upload/日期/时间戳_随机字符.扩展名`
- 数据库存储相对路径

### 2. 单例模式

- Banner和详情采用单例模式（只有一条记录）
- 保存时先查询是否存在，存在则更新，不存在则创建

### 3. 数据验证

- Banner：type必填，image/video根据type必填
- 详情：mainTitle必填
- 组织架构：至少需要一位主任
- 实验室：name和image必填

### 4. 删除处理

- 删除记录时**不删除**服务器上的文件
- 只删除数据库记录

---

## 📝 SQL初始化语句

将所有表的创建语句添加到 `scripts/init.sql` 文件中。

---

## ✅ 开发步骤

1. **数据库设计** - 在 `init.sql` 中添加表结构
2. **实体类** - 创建4个实体类
3. **Repository** - 创建4个Repository接口
4. **Service** - 创建CenterOverviewService，实现所有业务逻辑
5. **Controller** - 创建CenterOverviewController，实现所有API
6. **文件上传** - 实现文件上传工具类
7. **前端API调用** - 创建API服务文件
8. **前端页面** - 修改现有页面，对接API

---

## 🎯 总结

中心概况管理包含4个子模块，每个模块都有独立的CRUD功能。所有图片和视频都使用文件上传方式，存储在服务器文件系统中，数据库只存储相对路径。









